function serializeKey(...args) {
	return args.
}

function memoization(a, b) {
	const memKey = Array.from(arguments).toString();
	const 

	if (memoization.memory[memKey]) {
		const {a, b} = arguments
	}
}